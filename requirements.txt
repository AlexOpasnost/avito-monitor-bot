# All deps pinned to exact versions for reproducible builds and to
# limit supply-chain risk. Bumps go through CI (pip-audit) before
# landing — see .github/workflows/test.yml.
aiogram==3.15.0
asyncpg==0.30.0
httpx[http2]==0.27.2
orjson==3.11.8
python-dotenv==1.0.1
cloudscraper==1.2.71
curl_cffi==0.14.0
beautifulsoup4==4.12.3
tzdata==2026.2
deep-translator==1.11.4
mercapi==0.4.2
# Sentry: optional but enabled if SENTRY_DSN env var is set. Free
# tier (5k events/mo) covers a low-volume bot — without this, errors
# only surface in Railway logs.
sentry-sdk[aiohttp]==2.18.0
